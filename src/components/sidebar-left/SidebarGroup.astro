---
import SidebarItem from "./SidebarItem.astro";
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<div class="my-1.5 rounded-2xl bg-opacity-50">
  <SidebarItem text={title} isHeader={true} />
  <div class="ml-2 flex flex-col gap-1 text-sm">
    <slot />
  </div>
</div>
